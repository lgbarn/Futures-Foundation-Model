torch>=2.0
transformers>=4.30
peft>=0.10             # bolt_finetune --lora: parameter-efficient Chronos-Bolt fine-tune
datasets>=2.14
safetensors>=0.3
pandas>=2.0
numpy>=1.24,<2          # torch 2.x built against numpy 1.x ABI (_ARRAY_API); numpy 2 breaks DataLoader workers
scikit-learn>=1.3
pyyaml>=6.0
xgboost>=2.0
# ONNX export (produce export_onnx=True): XGBoost heads + Chronos encoder → ONNX
onnxmltools>=1.16
onnxruntime>=1.16
onnx>=1.16
onnxscript>=0.1
optuna>=3.0
joblib>=1.3
hmmlearn>=0.3              # regime HMM (futures_foundation.regime): market-state posteriors as head features
chronos-forecasting>=1.4   # pipelines/chronos: Chronos-Bolt backbone (Route C)
# futures_foundation.rl default PPO trainer (lazy-imported at train time only)
stable-baselines3>=2.0
gymnasium>=0.29